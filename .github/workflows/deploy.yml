# Baut den Next.js-Static-Export und veröffentlicht ihn auf GitHub Pages.
# Läuft nach jedem Push auf main – also auch nach jeder Änderung im CMS.
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      # Betrieb unter der GitHub-Pages-URL (kein DNS nötig):
      #   https://nick8952.github.io/gusto-campano-website/
      # Beim Go-Live auf die eigene Domain: public/CNAME anlegen, Custom Domain
      # in Settings → Pages setzen, dann hier
      #   SITE_ORIGIN: https://gusto-campano.ch
      #   BASE_PATH: ""
      # eintragen (Schritt für Schritt in HANDOVER.md, Abschnitt 5).
      SITE_ORIGIN: https://nick8952.github.io
      BASE_PATH: /gusto-campano-website
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
